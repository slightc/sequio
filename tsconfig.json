{
  // Root solution config: no files of its own — each package has its own
  // tsconfig extending `tsconfig.base.json`. Kept so editors resolve the
  // workspace as a whole.
  "files": [],
  "references": [
    { "path": "packages/engine" },
    { "path": "packages/server" },
    { "path": "packages/studio" }
  ]
}
